<ContentDialog
    x:Class="Navigation.UWP.Views.ResultView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:views="using:Navigation.UWP.Views"
    mc:Ignorable="d">

    <StackPanel>
        <TextBox Margin="4" Text="{Binding Path=Result, Mode=TwoWay}" />
        <Button Margin="4" Content="Close" Command="{Binding Path=CloseCommand}" HorizontalAlignment="Stretch" />
    </StackPanel>
</ContentDialog>